name: Generate Language Files

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: write

jobs:
  generate-languages:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Check TypeScript files
      run: |
        echo "🔍 Checking TypeScript syntax..."
        npx tsc --noEmit --skipLibCheck scripts/generate-languages.ts
        
    - name: Generate language files
      run: |
        echo "🚀 Generating language support files..."
        npx tsx scripts/generate-languages.ts
        
    - name: Verify generated files
      run: |
        echo "✅ Verifying generated files..."
        ls -la languages/
        echo "📊 File contents:"
        head -20 languages/languages.yml
        
    - name: Commit generated files
      if: github.event_name == 'push'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add languages/
        git diff --staged --quiet || git commit -m "Auto-generate language files"
        git push
